import { SiteHeader } from "../../components/SiteHeader";
import { siteMetadata } from "../../lib/site";
export const metadata = siteMetadata({ title: "Process", alternates: { canonical: "/process" } });
const steps = [["01", "Assessment", "Start with a free, no-obligation site assessment so the work can be understood in context."], ["02", "Written estimate", "Receive a written estimate that gives the next decision a clear shape."], ["03", "Plan + prepare", "Planning, permit, and inspection coordination happen alongside practical preparation, including dust barriers where needed."], ["04", "Build + care for the site", "Work proceeds with daily cleanup built into the rhythm of the job—not left until the end."]];
export default function ProcessPage() { return <><SiteHeader /><main id="main"><section className="page-hero"><div className="shell"><p className="eyebrow">Process</p><h1>Clear steps. Considered work.</h1><p className="hero-copy">A good project feels organized before it feels finished. SLK keeps the path visible from first look to final cleanup.</p></div></section><section className="section" aria-labelledby="steps-title"><div className="shell"><div className="section-header"><p className="eyebrow">How it works</p><div><h2 id="steps-title">A steadier way forward.</h2><p className="section-intro">The details will change from home to home. The care in the process stays consistent.</p></div></div><ol className="process-list">{steps.map(([number, title, body]) => <li key={number}><span className="step-number">{number}</span><div><h3>{title}</h3><p>{body}</p></div></li>)}</ol></div></section><section className="section dark"><div className="shell cta-strip"><div><p className="eyebrow">Begin simply</p><h2>Tell us what your home needs.</h2></div><a className="button primary" href="https://slkseamlessgutters.com/contact">Request a site assessment</a></div></section></main></>; }
